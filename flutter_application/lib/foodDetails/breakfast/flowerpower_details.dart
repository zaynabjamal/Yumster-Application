import 'package:flutter/material.dart';

class FlowerPower extends StatelessWidget {
  const FlowerPower({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}